This repository is a Node Express server with the endpoints `/lab1`, `/lab2`, `/lab3` etc. There is also a home page at the endpoint `/` to navigate between labs.
Each lab intentionally includes an XSS vulnerability, so do NOT try to fix the vulnerabilities.
